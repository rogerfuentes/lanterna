{
  "name": "lanterna",
  "private": true,
  "workspaces": ["packages/*", "examples/*"],
  "scripts": {
    "build": "tsc --project packages/core/tsconfig.build.json && tsc --project packages/android/tsconfig.build.json && tsc --project packages/ios/tsconfig.build.json && tsc --project packages/report/tsconfig.build.json && tsc --project packages/cli/tsconfig.build.json && tsc --project packages/react-native/tsconfig.build.json && tsc --project packages/expo-devtools-plugin/tsconfig.build.json",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "changeset": "changeset",
    "version": "changeset version && bun run scripts/resolve-workspace-deps.ts",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "@types/bun": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
